import type { DeckConfig } from "@svx-slides/core/deck/types";

const config = {
  title: "Réseaux sociaux numériques 5/5",
  theme: "gallery",
  template: {
    source: "deck.svx",
    slideSeparator: "---",
    notesSeparator: "--- notes",
  },
} satisfies DeckConfig;

export default config;
